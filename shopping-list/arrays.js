let users = {
    "00": "user_1",
    "01": "user_2",
    "02": "user_3"
}

let objusers = Object.values(users)
let objids = Object.keys(users)
let objentries = Object.entries(users)

console.log(objusers)